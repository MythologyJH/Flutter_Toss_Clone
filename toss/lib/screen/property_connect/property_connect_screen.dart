import 'package:flutter/material.dart';

import 'material/body.dart';

class PropertyConnectScreen extends StatelessWidget {

  const PropertyConnectScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}