import 'package:flutter/material.dart';

import 'material/body.dart';

class TossBankScreen extends StatelessWidget {

  const TossBankScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}